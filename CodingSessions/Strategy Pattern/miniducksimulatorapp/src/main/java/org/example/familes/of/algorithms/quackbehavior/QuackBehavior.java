package org.example.familes.of.algorithms.quackbehavior;

/**
 * QuackBehavior interface that represents a family of algorithms
 */
public interface QuackBehavior {
  /**
   * quack method that prints the quacking behavior
   */
  void quack();
}
